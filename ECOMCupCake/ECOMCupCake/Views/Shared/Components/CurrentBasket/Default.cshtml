@using System.Linq
@model IEnumerable<ECOMCupCake.Models.Basket>

@Model.Sum(item => item.Quantity) items in your Basket
@*
    @foreach (var item in Model)
    {
        <p>@item.Product.Name</p>
    }*@